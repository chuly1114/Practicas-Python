from math import factorial
import os

os.system("cls")

x=float(input("Introduzca el valor de x:"))
iterator_x = 0
e_to_the_x_power = 1 + x
factorial_x = 0
power=2


while iterator_x <= x :
    f = (int(input("Ingrese el número al que desea hallarle el factorial:")))
fac = 1

while f > 0:
    fac *= f
    f -= 1
    e_to_the_x_power+= (x**power)/

    